version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Verwende die Standard-Hugo-Version"
    build:
      commands:
        - hugo --minify
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []

